"""UI layer of the sCure DVT application.

    theme      one palette + one Qt stylesheet for the whole app
    widgets    Card, Pill, StatTile, Stepper, FadeStack, PulseDot, Toast
    dut        DUT (machine) client, live monitor thread, control panel
    wizard     the guided run wizard (one screen per step, animated)
"""
